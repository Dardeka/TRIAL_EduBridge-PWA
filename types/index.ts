export type NavItem = {
  title: string;
  href: string;
  disabled?: boolean;
};

export type Feature = {
  title: string;
  description: string;
  icon: string;
};

export type TechItem = {
  name: string;
  version: string;
};
